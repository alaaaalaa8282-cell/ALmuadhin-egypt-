package com.example.almuadhin.alarm

import android.app.AlarmManager
import android.app.PendingIntent
import android.app.Service
import android.content.Context
import android.content.Intent
import android.media.AudioAttributes
import android.media.AudioFocusRequest
import android.media.AudioManager
import android.media.MediaPlayer
import android.os.Build
import android.os.IBinder

class SalahSoundService : Service() {

    private var salahPlayer: MediaPlayer? = null
    private var audioManager: AudioManager? = null
    private var focusRequest: AudioFocusRequest? = null

    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        val prefs = getSharedPreferences("muadhin_settings", Context.MODE_PRIVATE)

        if (!prefs.getBoolean("salah_enabled", false)) {
            stopSelf()
            return START_NOT_STICKY
        }

        val soundName = prefs.getString("salah_sound", "NOZAKER") ?: "NOZAKER"
        val soundResId: Int = try {
            com.example.almuadhin.data.SalahSound.valueOf(soundName).resId
        } catch (e: Exception) {
            com.example.almuadhin.data.SalahSound.NOZAKER.resId
        }

        audioManager = getSystemService(AUDIO_SERVICE) as AudioManager

        val granted = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            val req = AudioFocusRequest.Builder(AudioManager.AUDIOFOCUS_GAIN_TRANSIENT)
                .setAudioAttributes(
                    AudioAttributes.Builder()
                        .setUsage(AudioAttributes.USAGE_MEDIA)
                        .setContentType(AudioAttributes.CONTENT_TYPE_MUSIC)
                        .build()
                )
                .build()
            focusRequest = req
            audioManager?.requestAudioFocus(req)
        } else {
            @Suppress("DEPRECATION")
            audioManager?.requestAudioFocus(
                null,
                AudioManager.STREAM_MUSIC,
                AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
            )
        }

        if (granted != AudioManager.AUDIOFOCUS_REQUEST_GRANTED) {
            stopSelf()
            return START_NOT_STICKY
        }

        salahPlayer?.release()
        salahPlayer = MediaPlayer.create(this, soundResId)
        salahPlayer?.start()
        salahPlayer?.setOnCompletionListener {
            releaseAudioFocus()
            it.release()
            salahPlayer = null
            stopSelf()
        }

        return START_NOT_STICKY
    }

    private fun releaseAudioFocus() {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
            focusRequest?.let { audioManager?.abandonAudioFocusRequest(it) }
        } else {
            @Suppress("DEPRECATION")
            audioManager?.abandonAudioFocus(null)
        }
    }

    override fun onBind(intent: Intent?): IBinder? = null

    override fun onDestroy() {
        releaseAudioFocus()
        salahPlayer?.release()
        salahPlayer = null
        super.onDestroy()
    }
}
